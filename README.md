# Deno Backend Template

This repository will be a template for creating backend for future projects.

## Before running
Before you run the project, you would likely need to run this command:
`deno install`

## Available resources
- File based routing (subrouting included).
- Route controllers
- Models
- Socket.io and custom event handlers

## Development
`deno task dev`
